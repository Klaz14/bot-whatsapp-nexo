{
  "name": "bot-tt-kalaza",
  "version": "1.0.0",
  "private": true,
  "description": "Bot que reenvía imágenes y PDFs de grupos específicos de WhatsApp a una carpeta de Google Drive (deployment kalaza).",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "auth": "node auth.js",
    "test": "node scripts/test-offline.js",
    "setup:chrome": "npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "googleapis": "^144.0.0",
    "node-poppler": "^9.1.2",
    "p-limit": "^3.1.0",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.26.0"
  }
}
